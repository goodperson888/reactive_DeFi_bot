# 项目诊断报告

> 生成时间：2026-03-25
> 项目：Reactive DeFi Bot
> 报告内容：问题清单 + 功能定位 + 修复路线图

---

## 一、项目期望实现的功能

本项目是一个**基于 Reactive Network 的跨链 DeFi 自动化机器人**，利用 Reactive Network 的链上事件监听与跨链回调能力，在无需服务器的情况下自动执行清算和套利操作。

### 整体架构

```
Origin 链 (Sepolia)
  MockLending  ──emit HealthFactorUpdated──>
  MockDEX A    ──emit Swap──────────────────>
                                              Reactive Network
                                                RCController
                                                - 订阅并接收跨链事件
                                                - 判断触发条件
                                                - emit Callback 触发执行
                                                     |
                                                     | 跨链 Callback
                                                     v
                                             Destination 链 (Base Sepolia)
                                               LiquidationExecutor  (执行清算)
                                               ArbitrageExecutor    (执行套利)
```

**Reactive Network 的核心价值**：Origin 链的事件就是状态变化的载体，RC 通过订阅事件自动获得跨链信息，无需预言机或 CCIP，这是 Reactive Network 的原生设计。

### 场景 1：自动清算机器人

```
用户在 MockLending 存入抵押品并借款
  → ETH 价格下跌，健康度低于阈值（1.02）
  → MockLending emit HealthFactorUpdated 事件
  → RCController 跨链捕获事件，判断健康度 < 1.02
  → 跨链 Callback 调用 Base Sepolia 的 LiquidationExecutor
  → 执行清算，获得 5% 清算奖励
```

### 场景 2：跨链套利机器人

```
两条链的 DEX 出现价差（如 Origin 3000 USDC/ETH，Destination 3050 USDC/ETH）
  → Origin 链有用户执行 Swap，触发 Swap 事件
  → RCController 跨链捕获事件，计算价差 > 1.5%
  → 跨链 Callback 调用 Base Sepolia 的 ArbitrageExecutor
  → 在 Destination 链执行套利交易获利
```

---

## 二、发现的问题

### 2.1 严重问题（导致功能完全失效）

#### 问题 1：事件 Topic 哈希是占位符，完全错误

- **位置**：`contracts/reactive/RCController.sol` 第 99-104 行
- **现象**：`HEALTH_FACTOR_UPDATED_TOPIC` 和 `SWAP_TOPIC` 的值是明显的重复字节占位符，不是真正的 keccak256 哈希
- **影响**：RCController 订阅错误的事件签名，永远无法匹配任何链上事件，整个系统不会运行

```solidity
// 当前错误值（重复字节，明显是占位符）
uint256 private constant HEALTH_FACTOR_UPDATED_TOPIC =
    0x8c0d6b0b6f3c3e3c3e3c3e3c3e3c3e3c3e3c3e3c3e3c3e3c3e3c3e3c3e3c3e3c;
uint256 private constant SWAP_TOPIC =
    0x7c0d6b0b6f3c3e3c3e3c3e3c3e3c3e3c3e3c3e3c3e3c3e3c3e3c3e3c3e3c3e3c;

// 应改为正确的 keccak256 值
// keccak256("HealthFactorUpdated(address,uint256,uint256,uint256)")
// keccak256("Swap(address,address,address,uint256,uint256,uint256)")
```

---

#### 问题 2：Swap 事件数据解析错误

- **位置**：`contracts/reactive/RCController.sol` 第 231-234 行
- **现象**：从 `log.data` 解码了 6 个值（3 个 address + 3 个 uint256），但 Swap 事件前 3 个参数（user、tokenIn、tokenOut）是 `indexed`，存储在 topics 中，`log.data` 只含 3 个 uint256
- **影响**：解码失败或得到错误数据，套利逻辑永远无法正确触发

```solidity
// 当前错误写法（把 indexed 字段当 data 解析）
(, , , , , uint256 newPrice) = abi.decode(
    log.data,
    (address, address, address, uint256, uint256, uint256)
);

// 正确写法（data 中只有非 indexed 的 3 个 uint256）
(uint256 amountIn, uint256 amountOut, uint256 newPrice) =
    abi.decode(log.data, (uint256, uint256, uint256));
```

---

#### 问题 3：部署脚本网络名称不匹配

- **位置**：`scripts/deploy/deploy-all.js` 第 52、90 行
- **现象**：`hardhat.config.js` 定义的网络名与部署脚本调用的名称不一致

| 链 | hardhat.config.js 定义 | deploy-all.js 使用 | 结果 |
|---|---|---|---|
| Base Sepolia | `base_sepolia`（下划线） | `"base-sepolia"`（连字符） | 报错 |
| Reactive Network | `lasna` | `"reactive"` | 报错 |

---

#### 问题 4：`hre.changeNetwork` 方法不存在

- **位置**：`scripts/deploy/deploy-all.js` 第 28、52、65、90、112 行
- **现象**：`hre.changeNetwork()` 不是 Hardhat 内置方法，需要 `hardhat-change-network` 插件，但 `package.json` 未安装
- **影响**：部署脚本第一次调用 `changeNetwork` 时就崩溃，完全无法运行

---

#### 问题 5：测试逻辑与实际合约实现完全不匹配

- **位置**：`test/Executors.test.js`
- **现象 1**：测试使用随机钱包地址作为 DEX/Lending 合约地址，但实际合约会对这些地址发起真实调用，必然 revert
- **现象 2**：测试断言了特定利润值（5%、1.5%），但合约没有这样的固定计算逻辑

```javascript
// 测试中：随机地址，没有合约代码
const targetContract = hre.ethers.Wallet.createRandom().address;

// 合约中：真实调用这个地址，会 revert
uint256 hf = ILending(targetContract).getHealthFactor(targetUser);
```

---

### 2.2 逻辑问题（代码缺陷，非架构问题）

#### 问题 6：Executor 内部做了多余的跨链二次验证

- **位置**：`contracts/destination/LiquidationExecutor.sol` 第 71 行，`contracts/destination/ArbitrageExecutor.sol` 第 90 行
- **说明**：Reactive Network 已经通过事件订阅将 Origin 链的状态传递给了 RC，RC 判断后才触发 Callback。Executor 不需要再次调用源链合约验证。但当前代码在 Base Sepolia 上对 Sepolia 的合约地址发起调用，那些地址在 Base Sepolia 上没有合约代码，会 revert
- **修复方式**：删除多余验证，RC 已将必要数据放入 payload 传给 Executor，Executor 直接信任即可

#### 问题 7：`vm` 变量在本地测试环境恒为 `false`

- **位置**：`contracts/reactive/RCController.sol` 第 55-58 行
- **说明**：在真实 Reactive Network 节点上，`vm` 由 ReactVM 自动设置为 `true`，这是 Reactive Network 的设计机制。本地 Hardhat 测试时 `vm` 永远是 `false`，`vmOnly` 修饰符导致 `react()` 永远无法调用
- **修复方式**：本地测试中需要 mock `vm = true`，或单独测试各内部函数

---

### 2.3 中等问题

#### 问题 8：package.json 缺少模块类型声明

- **位置**：`package.json`
- **现象**：`config/index.js`、`scripts/deploy/deploy-all.js`、测试文件使用 ES 模块语法（`import/export`），但 `package.json` 没有 `"type": "module"`，而 `hardhat.config.js` 使用 CommonJS（`require()`）
- **影响**：两种模块系统混用，运行时可能报 `SyntaxError: Cannot use import statement`

#### 问题 9：WORKFLOW.md 描述的逻辑与实际合约不符

- **位置**：`WORKFLOW.md` 第 283-285 行
- **现象**：文档描述 LiquidationExecutor 做简单的 5% 固定计算，但实际合约调用真实的 `liquidate()` 函数并通过余额差计算利润

---

### 2.4 次要问题

#### 问题 10：文件名拼写错误

- **位置**：项目根目录
- `hlper-hardhat-config.js` 应为 `helper-hardhat-config.js`（缺少字母 `e`）

#### 问题 11：`_calculateSpread` 存在除零风险

- **位置**：`contracts/reactive/RCController.sol` 第 282-288 行
- **现象**：当 `priceA == 0` 时，`else` 分支执行 `/ priceA`，导致除零 revert

---

## 三、修复后能否实现功能

### 结论

**修复后可以完整实现项目设计的所有功能。**

Reactive Network 本身已经原生支持跨链状态感知，项目架构方向完全正确。当前的问题都是代码层面的 Bug，没有需要重新设计的架构问题。

### 修复后的能力边界

```
可以完整实现：
  - 跨链事件监听（RC 订阅 Sepolia 事件，Reactive Network 原生支持）
  - 自动判断清算条件（健康度阈值）和套利条件（价差阈值）
  - 无需服务器，24/7 链上自动运行
  - 跨链 Callback 自动调用 Base Sepolia 的 Executor
  - Executor 执行清算/套利并记录利润

当前 Mock 演示的局限（与 Reactive Network 无关，属于设计范围）：
  - MockDEX 没有真实 ERC-20 代币转账（演示用，可接受）
  - 生产环境需要更完善的资金安全机制
```

### 所有问题的修复方式

| 问题 | 修复方式 |
|---|---|
| Topic 哈希错误 | 填入正确的 keccak256 值 |
| Swap 数据解析错误 | 修正 abi.decode 参数类型 |
| 部署脚本网络名 | 统一改为 `base_sepolia`、`lasna` |
| hre.changeNetwork | 拆分为 3 个独立部署脚本 |
| 测试不匹配 | 重写测试，使用真实 Mock 合约 |
| Executor 二次验证 | 删除多余的跨链调用，信任 RC payload |
| vm 本地测试 | mock vm 状态，或测试内部函数 |
| 模块系统混用 | package.json 加 `"type": "module"` 或统一改为 CommonJS |
| 文档不符 | 更新 WORKFLOW.md |
| 文件名拼写 | 重命名文件 |
| 除零风险 | 添加 `priceA > 0` 检查 |

---

## 四、修复路线图

### 第一步：修复合约 Bug（优先级：最高）

1. 计算并填入正确的事件 Topic 哈希
   - `keccak256("HealthFactorUpdated(address,uint256,uint256,uint256)")`
   - `keccak256("Swap(address,address,address,uint256,uint256,uint256)")`
2. 修正 `_handleSwapEvent` 中的 `abi.decode` 参数
3. 删除 `LiquidationExecutor` 中对 `getHealthFactor` 的多余调用
4. 删除 `ArbitrageExecutor` 中对 `dexA.getAmountOut` 的多余调用
5. 在 `_calculateSpread` 中添加除零保护

### 第二步：重构部署脚本（优先级：高）

将 `scripts/deploy/deploy-all.js` 拆分为三个独立脚本：

```
scripts/deploy/
  01-deploy-origin.js       # --network sepolia
  02-deploy-destination.js  # --network base_sepolia
  03-deploy-reactive.js     # --network lasna
```

部署顺序：
```bash
npx hardhat run scripts/deploy/01-deploy-origin.js --network sepolia
npx hardhat run scripts/deploy/02-deploy-destination.js --network base_sepolia
npx hardhat run scripts/deploy/03-deploy-reactive.js --network lasna
```

### 第三步：重写测试（优先级：中）

使用真实部署的 Mock 合约（MockDEX、MockLending）替换随机地址，确保测试能真实验证合约逻辑。对 `react()` 的测试需要 mock `vm = true`。

### 第四步：收尾（优先级：低）

- 修正文件名 `hlper-hardhat-config.js` → `helper-hardhat-config.js`
- 统一模块系统（推荐全部改为 ESM，package.json 加 `"type": "module"`）
- 更新 WORKFLOW.md 使其与实际代码一致

---

## 五、问题汇总

| 编号 | 问题 | 严重程度 | 是否影响运行 |
|---|---|---|---|
| 1 | 事件 Topic 哈希是占位符 | 严重 | 是，系统无法工作 |
| 2 | Swap 事件数据解析错误 | 严重 | 是，套利逻辑失效 |
| 3 | 部署脚本网络名不匹配 | 严重 | 是，无法部署 |
| 4 | hre.changeNetwork 不存在 | 严重 | 是，无法部署 |
| 5 | 测试与合约逻辑不匹配 | 严重 | 是，测试全部失败 |
| 6 | Executor 多余的跨链二次验证 | 逻辑 | 是，执行时 revert |
| 7 | vm 变量本地测试恒为 false | 设计 | 是，本地无法测试 react() |
| 8 | 模块系统混用 | 中等 | 可能，取决于 Hardhat 版本 |
| 9 | 文档与代码不符 | 中等 | 否 |
| 10 | 文件名拼写错误 | 次要 | 否 |
| 11 | 除零风险 | 次要 | 边缘情况下 revert |
